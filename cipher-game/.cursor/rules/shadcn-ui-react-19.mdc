---
description: Resolve compatibility issues between shadcn/ui and React 19
globs: components/ui/**/*.*
---

If you're using npm, install shadcn/ui dependencies with a `--force` flag to avoid a conflict:

```bash
npm i <package> --force
```
